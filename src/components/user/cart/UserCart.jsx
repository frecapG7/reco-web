import { Box, Typography } from "@mui/material";

export const UserCart = ({ cart }) => {
  return (
    <Box>
      <Typography variant="h4" align="center">
        Cart
      </Typography>
    </Box>
  );
};
