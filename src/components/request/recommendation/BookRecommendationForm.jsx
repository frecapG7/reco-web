import { forwardRef } from "react";

export const BookRecommendationForm = forwardRef(
  ({ onSubmit, recommendation }, ref) => {
    return <>
    
    </>;
  }
);

BookRecommendationForm.displayName = "BookRecommendationForm";
