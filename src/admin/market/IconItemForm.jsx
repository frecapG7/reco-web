import { Box } from "@mui/material";
import { FormUpload } from "../../components/form/FormUpload";

export const IconItemForm = ({ control }) => {
  return (
    <Box aria-label="icon-item-form-part">
      
    </Box>
  );
};
